{
  "name": "livecontent-studio-ui",
  "private": true,
  "description": "PDF.js is the only dependency. The page is served as static files with no build step, so the browser build is committed under vendor/. Run `npm run vendor` after changing the pinned version.",
  "scripts": {
    "vendor": "npm install && cp node_modules/pdfjs-dist/legacy/build/pdf.min.mjs node_modules/pdfjs-dist/legacy/build/pdf.worker.min.mjs vendor/"
  },
  "dependencies": {
    "pdfjs-dist": "6.3.289"
  }
}
